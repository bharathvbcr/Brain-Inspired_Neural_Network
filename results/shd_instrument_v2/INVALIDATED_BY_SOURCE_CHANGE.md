# Calibration artifact status

**Status: `INVALID_HARNESS`**

V2 completed canonical data summaries, strict Python/Rust parity, and
fresh-process replay successfully. Reference setup then exposed an environment
target-path bug before any reference or matrix cell ran.

The v2 artifacts remain useful mechanical audit evidence but cannot authorize
scientific execution after the runner correction. The authoritative runner will
freeze a new manifest under `results/shd_instrument_v3/`.
