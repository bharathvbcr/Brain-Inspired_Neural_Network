# BINN overnight gap fixes — 2026-07-25

## Outcome

The pre-fix protocol-v141 SHD report is preserved at
`results/runs/2026-07-25_195647_overnight/shd_arch_ablation_h128.md`.
It is `INVALID_HARNESS`: the worst DFA/e-prop hidden-modulator RMS ratio was
11.94 against the preregistered 3.5 limit. Its H1/H2 numbers are non-claims.

The confirmed implementation and orchestration defects were fixed without
weakening thresholds:

1. Multi-channel neuromodulation no longer multiplies RPE twice. DA, ACh and NE
   preserve the directional sign, and explicit channel components make the ACh
   mechanism directly testable.
2. The inhibitory area now has deterministic heterogeneous E→I and I→E
   projections instead of broadcasting one scalar to all excitatory cells.
3. SHD ALIF input and recurrent synapses now carry both `ε_v` and `ε_a`;
   fixed-threshold arms use the same implementation with `β_a = 0`.
4. DFA and transported e-prop hidden modulators are RMS-matched to the frozen
   initialization scale while preserving direction and output-error magnitude.
5. Quick and learning-rate-pilot SHD schedules are forced to
   `UNDERPOWERED`; only a confirmatory schedule can emit H1/H2 `PASS` or `FAIL`.
6. The overnight scheduler now has a global PID lock, source fingerprint,
   durable per-job status, a portable Python process-group timeout, and a
   summary that distinguishes execution status from scientific verdicts.

## Verification

- `cargo check -p binn-learn -p binn-areas -p binn-lab --all-targets`: pass.
- Focused ALIF, neuromodulation, inhibition, and report-guard tests: pass.
- Portable timeout regression: timed command returned 124; ordinary exit 7
  remained 7.
- Two clean `./scripts/overnight.sh --smoke --force` runs passed every Tier-0
  and Tier-1 execution job:
  - `results/runs/2026-07-25_205130_overnight/SUMMARY.md`
  - `results/runs/2026-07-25_205816_overnight/SUMMARY.md`
- Protocol-v142 SHD quick:
  - no degenerate cell;
  - negative control clean;
  - worst modulator ratio 1.00;
  - H1/H2 correctly `UNDERPOWERED`.
- Neuromodulation protocol v136: P1–P4 each 20/20 `PASS`.
- E/I protocol v135: C1–C3 `PASS`; measured competition spread 0.162–0.640.

Focused report artifacts are under `results/runs/2026-07-25_gapfix/`.

## Remaining boundary

- Protocol v142 has not yet completed its default confirmatory Tier-2 run.
  The quick report is validation evidence only and must not be cited as an
  architecture result.
- Tier 2–4 entries remain `pending` in the smoke summaries.
- Deep-SNN scaling remains `UNDERPOWERED` on the two-input
  `CoincidenceTask`; one depth-matched ceiling is inverted. Moving that suite
  to an input-rich task is a separate preregistered experimental redesign, not
  a threshold or label fix.
- `cargo fmt --all -- --check` remains red because the broad pre-existing dirty
  worktree contains formatting drift across many unrelated files. The Rust
  files changed by this gap fix were formatted directly; unrelated concurrent
  edits were not mass-rewritten.
